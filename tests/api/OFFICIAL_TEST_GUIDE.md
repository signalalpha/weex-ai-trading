# WEEX AI Trading 官方 API 测试指南

根据 [官方文档](https://www.weex.com/zh-CN/news/detail/ai-wars-weex-alpha-awakens-weex-global-hackathon-api-test-process-guide-266016) 完成赛前 API 测试任务。

## 测试要求

✅ **必须完成至少 10 USDT 的交易**，才能获得正式参赛资格。

## 测试步骤

脚本 `official_api_test.py` 会自动完成以下步骤：

1. ✅ **检查账户余额** - 确认测试账户有 1000 USDT 测试资金
2. ✅ **设置杠杆** - 设置为 1x（全仓模式）
3. ✅ **获取资产价格** - 获取 `cmt_btcusdt` 的当前价格
4. ✅ **下单** - 执行至少 10 USDT 的市价单
5. ✅ **查询当前委托** - 查看未完成订单
6. ✅ **查询历史委托** - 查看历史订单记录
7. ✅ **查询交易详情** - 查看已完成交易的详细信息

## 使用方法

### 1. 准备环境

确保已安装依赖：

```bash
pip install -r requirements.txt
```

### 2. 配置 API 凭证

在当前目录创建 `.env` 文件：

```bash
cat > .env << EOF
WEEX_API_KEY=your_api_key
WEEX_SECRET_KEY=your_secret_key
WEEX_PASSPHRASE=your_passphrase
EOF
```

### 3. 运行测试

```bash
python3 official_api_test.py
```

## 预期结果

如果所有步骤都成功完成，你应该看到：

- ✅ 账户余额查询成功
- ✅ 杠杆设置成功
- ✅ 价格获取成功
- ✅ 订单提交成功（订单ID会显示）
- ✅ 当前委托查询成功
- ✅ 历史委托查询成功
- ✅ 交易详情查询成功

## 注意事项

1. **测试账户**: 每个测试账户有 1000 USDT 测试资金
2. **交易对**: 使用 `cmt_btcusdt` 进行测试
3. **订单类型**: 使用市价单（`order_type: "0"`），对手价（`match_price: "1"`）
4. **交易金额**: 脚本默认下单 10 USDT
5. **杠杆设置**: 脚本会将杠杆设置为 1x（全仓模式）

## 常见问题

### Q: 如果某个步骤失败怎么办？

A: 脚本会继续执行后续步骤。如果关键步骤（如下单）失败，请检查：
- API 凭证是否正确
- 账户余额是否充足
- 网络连接是否正常
- IP 是否已添加到 API 白名单

### Q: 如何确认订单已成功执行？

A: 查看"交易详情查询"的结果，应该能看到刚刚完成的交易记录。

### Q: 可以修改下单金额吗？

A: 可以，编辑 `official_api_test.py` 文件中的 `order_value = 10.0` 参数，但必须至少 10 USDT。

## 完成测试后

✅ 完成所有测试步骤后，你就正式获得了参赛资格！

可以开始开发你的 AI 交易策略了。祝你在 WEEX AI Trading 黑客松中取得好成绩！🚀

