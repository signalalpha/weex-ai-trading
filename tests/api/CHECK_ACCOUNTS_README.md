# WEEX 账户批量检查工具

用于批量检查 WEEX 账户的余额和交易量，快速了解账户状态。

## 功能特性

- ✅ **批量检查账户余额** - 快速查看所有账户的 USDT 余额
- ✅ **批量检查交易量** - 检查每个账户是否达到指定的最小交易量要求（默认 10 USDT）
- ✅ **支持代理** - 可通过代理访问，解决 IP 白名单问题
- ✅ **灵活配置** - 支持 JSON 和 CSV 格式的 API keys 文件
- ✅ **结果导出** - 可导出检查结果到 JSON 文件

## 安装依赖

```bash
pip install -r requirements.txt
```

## 使用方法

### 1. 检查余额和交易量（默认最小交易量 10 USDT）

```bash
python3 check_accounts.py --api-keys api_keys.csv --proxy http://proxy.example.com:3128
```

### 2. 自定义最小交易量要求

```bash
python3 check_accounts.py --api-keys api_keys.csv --min-volume 20 --proxy http://proxy.example.com:3128
```

### 3. 仅检查余额（不检查交易量）

```bash
python3 check_accounts.py --api-keys api_keys.csv --balance-only --proxy http://proxy.example.com:3128
```

### 4. 指定交易对

```bash
python3 check_accounts.py --api-keys api_keys.csv --symbol cmt_ethusdt --proxy http://proxy.example.com:3128
```

### 5. 导出检查结果

```bash
python3 check_accounts.py --api-keys api_keys.csv --output check_results.json --proxy http://proxy.example.com:3128
```

## 命令行参数

```
--api-keys FILE       API keys 文件路径（JSON 或 CSV 格式），必需
--proxy URL           代理地址，例如: http://proxy.example.com:3128
--min-volume FLOAT    最小交易量要求（USDT），默认: 10.0
--symbol STRING       交易对符号，默认: cmt_btcusdt
--balance-only        仅检查余额，不检查交易量
--output FILE         输出结果到 JSON 文件（可选）
```

## API Keys 文件格式

与 `official_api_test_batch.py` 使用相同的文件格式。

### CSV 格式

```csv
api_key,secret_key,passphrase
weex_xxx,xxx,xxx
weex_yyy,yyy,yyy
```

### JSON 格式

```json
[
  {
    "api_key": "weex_xxx",
    "secret_key": "xxx",
    "passphrase": "xxx"
  },
  {
    "api_key": "weex_yyy",
    "secret_key": "yyy",
    "passphrase": "yyy"
  }
]
```

## 输出示例

### 控制台输出

```
📁 从文件加载 API keys: api_keys.csv
✅ 加载了 10 个 API key

🌐 使用代理: http://proxy.example.com:3128

📊 最小交易量要求: 10.0 USDT
📈 交易对: cmt_btcusdt

[1/10] 检查账户: weex_21723ca5c3...
  USDT余额: 1000.00
  ✅ 交易量: 15.50 USDT (3笔交易) ≥ 10.0 USDT

[2/10] 检查账户: weex_ffb08c7543...
  USDT余额: 1000.00
  ⚠️  交易量: 5.20 USDT (1笔交易) < 10.0 USDT

...

================================================================================
检查结果统计
================================================================================
余额检查成功: 10/10
交易量检查成功: 10/10
交易量达到要求 (≥10.0 USDT): 7/10
```

### JSON 输出格式

```json
{
  "check_time": "2025-01-01T12:00:00",
  "total_count": 10,
  "balance_success_count": 10,
  "min_volume": 10.0,
  "symbol": "cmt_btcusdt",
  "volume_meets_count": 7,
  "proxy": "http://proxy.example.com:3128",
  "results": [
    {
      "api_key": "weex_xxx",
      "balance_check": {
        "success": true,
        "balance": [...],
        "usdt_balance": 1000.0,
        "error": null
      },
      "volume_check": {
        "success": true,
        "total_volume": 15.5,
        "trade_count": 3,
        "meets_requirement": true,
        "error": null
      }
    },
    ...
  ]
}
```

## 使用场景

1. **赛前检查** - 批量检查所有账户是否达到 10 USDT 的交易量要求
2. **账户监控** - 定期检查账户余额，了解资金状态
3. **交易统计** - 查看各账户的交易量和交易次数
4. **问题排查** - 快速定位余额不足或交易量未达标的账户

## 注意事项

1. **交易量计算** - 交易量基于交易历史记录（`/capi/v2/order/fills`）计算，仅统计指定交易对的数据
2. **API 限制** - 脚本会在请求之间添加延迟，避免触发 API 限流
3. **代理配置** - 如果使用代理，确保代理 IP 已添加到 WEEX API 白名单
4. **数据准确性** - 交易量计算可能因 API 返回字段名不同而略有差异，建议结合实际测试验证

## 与测试脚本的区别

| 功能 | check_accounts.py | official_api_test_batch.py |
|------|------------------|---------------------------|
| 用途 | 快速检查账户状态 | 完整的 API 测试流程 |
| 检查余额 | ✅ | ✅ |
| 检查交易量 | ✅ | ❌ |
| 下单测试 | ❌ | ✅ |
| 查询委托 | ❌ | ✅ |
| 运行速度 | 快（只查询） | 慢（包含下单等操作） |

## 常见问题

### Q: 交易量计算不准确？

A: 交易量基于 API 返回的交易记录计算。如果结果不准确，可能是：
- API 返回的字段名与脚本预期不符
- 需要查询更多历史记录（当前只查询最近 100 条）
- 交易对选择不正确

### Q: 如何只检查余额？

A: 使用 `--balance-only` 参数：
```bash
python3 check_accounts.py --api-keys api_keys.csv --balance-only --proxy http://proxy.example.com:3128
```

### Q: 可以检查多个交易对的交易量吗？

A: 当前版本一次只能检查一个交易对。如果需要检查多个交易对，可以多次运行脚本，每次指定不同的 `--symbol` 参数。

